import footer, { show, hide } from "@/store/slices/footerSlice";
import scroll, { black, white, fix, flex } from "@/store/slices/scrollSlice";

export { footer, show, hide };
export { scroll, black, white, fix, flex };
